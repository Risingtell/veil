# Node
node_modules/

# Circom/snarkjs build artifacts (large / regenerable)
build/*.ptau
build/*.zkey
build/*.r1cs
build/*.sym
build/withdraw_js/
build/*.wtns

# keep: build/verification_key.json, build/proof.json, build/public.json,
#       build/soroban_inputs.json  (small, useful to inspect)

# Rust
contracts/**/target/
target/

# large local tool binaries; documented in README how to fetch them
bin/circom.exe
bin/stellar.exe
bin/*.tar.gz

# misc
.DS_Store
*.log
